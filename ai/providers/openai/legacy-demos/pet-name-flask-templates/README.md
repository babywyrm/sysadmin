# Pet Name Flask Support Files

This directory contains template and dependency files for an old OpenAI
quickstart-style Flask demo.

`requirements.legacy.txt` is retained for historical reproducibility, but it is
not a maintained dependency set. Modernize the demo before running it on a shared
machine or network.
